# Golden File Final Verification

This checklist is the Phase 11 evidence register. The 2026-07-10 live audit reopened Phase 11;
checked items prove only the statement they contain, not overall completion.

## Structural Evidence

- [x] A TS/TSX path inventory exists and is reproducibly generated by `scripts/generate-golden-file-inventory.mjs`.
- [x] `bun run check:golden` verifies that this inventory has no missing or stale TS/TSX rows.
- [ ] Inventory scope includes persistence artifacts, source-controlled tooling, and runtime/build configuration required by the master plan.
- [ ] Inventory classifications record domain/layer/rationale/owner/evidence and are semantically validated.
- [x] Superseded root `ExecPlan.md` archived at `docs/legacy-exec-plan.md`.
- [x] Legacy feature paths audited with repository-wide searches.
- [ ] Golden convention checker passes with zero transitional architectural allowlist entries.
- [ ] Checker covers indirect orchestration, props-only views, container/hook/view-model shape, router output parsing, and repo-wide feature ownership.
- [ ] Every feature has a complete canonical layer matrix with direct evidence or a justified `N/A`.

Current blockers include five allowlisted task/time-tracking/shared views, Billing and Team views that
call orchestration hooks indirectly, a monolithic Notifications surface, and raw error
normalization in presentational views.

## Commands

| Command                  | Current result                       | Validation meaning                                                         |
| ------------------------ | ------------------------------------ | -------------------------------------------------------------------------- |
| `bun run check`          | Pass with 3 lint warnings            | Canonical lint/convention/format command is green                          |
| `bun run check-types`    | Pass; 8 Turbo tasks                  | Current configured package type tasks are green                            |
| `bun run check:golden`   | Pass; 646 TS/TSX paths               | Path coverage only; classification and broader scope remain unproved       |
| `bun run check:realtime` | Pass; 5 event names                  | Metadata presence only; referenced paths/tests are not validated           |
| `bun run check:unused`   | **Fail**                             | Knip reports 72 files, 37 dependencies, 10 exports/types, and 27 hints     |
| `bun test`               | Pass; 192 tests, 47 discovered files | Includes a generated `apps/server/dist` test; source-only proof is pending |
| `bun run db:generate`    | Pass; no schema changes              | A clean post-command diff is still required as the durable drift proof     |

## Remaining Gates

- [ ] Expand and semantically validate the inventory; create the per-feature canonical matrix.
- [ ] Remove all five transitional view exceptions and indirect view orchestration.
- [ ] Complete the unchecked generic-error, web-error, naming, and Team/Workspace authorization reviews in `docs/golden-file-auth-membership-audit.md`.
- [ ] Prove Zod output parsing for every product router and actor-first input-object service signatures where the canonical rule applies.
- [ ] Fix `knip.json` for React/Vite and make `bun run check:unused` green or record an explicit plan-level scope decision for each owned exception.
- [ ] Strengthen realtime validation so registry producer, consumer, notification, and test paths exist and cover the named event.
- [ ] Exclude generated output from test discovery and run the required suite in CI with its database fixture.
- [ ] Reconcile moved exemplar paths and resolved cleanup notes in `docs/golden-file-pattern.md`.
- [ ] Re-run every command and audit the post-command worktree before marking Phase 11 complete.
