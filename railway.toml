[build]
buildCommand = "bun install && bun run build"

[deploy]
preDeployCommand = "bun run --cwd packages/db db:push"
startCommand = "bun run start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
