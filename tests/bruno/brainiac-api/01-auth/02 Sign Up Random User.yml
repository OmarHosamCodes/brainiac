info:
  name: Sign Up Random User
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-up/email"
  headers:
    - name: content-type
      value: application/json
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: json
    data: |-
      {
        "name": "Bruno Test User",
        "email": "{{randomSignupEmail}}",
        "password": "{{seedPassword}}"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Sign up succeeds", function() {
          expect([200, 201]).to.include(res.status);
        });
