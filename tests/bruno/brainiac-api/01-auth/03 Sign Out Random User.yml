info:
  name: Sign Out Random User
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-out"
  headers:
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("Sign out succeeds", function() {
          expect([200, 204]).to.include(res.status);
        });
