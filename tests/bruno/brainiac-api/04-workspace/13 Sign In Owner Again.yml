info:
  name: Sign In Owner Again
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-in/email"
  headers:
    - name: content-type
      value: application/json
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: json
    data: |-
      {
        "email": "{{ownerEmail}}",
        "password": "{{seedPassword}}"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Owner signs back in", function() {
          expect(res.status).to.equal(200);
        });
