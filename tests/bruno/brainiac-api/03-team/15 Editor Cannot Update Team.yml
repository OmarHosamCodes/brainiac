info:
  name: Editor Cannot Update Team
  type: http
  seq: 19
http:
  method: POST
  url: "{{rpcBaseUrl}}/team/update"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "json": {
          "teamId": "{{teamId}}",
          "name": "Editor Attempt {{testRunId}}"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Non-owner cannot rename team", function() {
          expect([401, 403]).to.include(res.status);
        });
