info:
  name: Sign Out Editor
  type: http
  seq: 21
http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-out"
  headers:
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("Editor sign out succeeds", function() {
          expect([200, 204]).to.include(res.status);
        });
