info:
  name: Sign Up Viewer
  type: http
  seq: 8
http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-up/email"
  headers:
    - name: content-type
      value: application/json
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: json
    data: |-
      {
        "name": "Bruno Viewer",
        "email": "{{viewerEmail}}",
        "password": "{{seedPassword}}"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Viewer signup succeeds", function() {
          expect([200, 201]).to.include(res.status);
          expect(res.body).to.be.an("object");
          expect(res.body.user).to.be.an("object");
          expect(res.body.user.id).to.be.a("string");

          bru.setVar("viewerUserId", res.body.user.id);
        });
