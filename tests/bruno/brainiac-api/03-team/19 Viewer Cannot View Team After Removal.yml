info:
  name: Viewer Cannot View Team After Removal
  type: http
  seq: 23
http:
  method: POST
  url: "{{rpcBaseUrl}}/team/get"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "json": {
          "teamId": "{{teamId}}"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Removed user cannot access team", function() {
          expect([401, 403, 404]).to.include(res.status);
        });
