info:
  name: Sign In Viewer
  type: http
  seq: 22
http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-in/email"
  headers:
    - name: content-type
      value: application/json
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: json
    data: |-
      {
        "email": "{{viewerEmail}}",
        "password": "{{seedPassword}}"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Viewer sign in succeeds", function() {
          expect(res.status).to.equal(200);
        });
