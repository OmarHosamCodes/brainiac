info:
  name: Sign Out Existing Session
  type: http
  seq: 1
http:
  method: POST
  url: "{{baseUrl}}/api/auth/sign-out"
  headers:
    - name: origin
      value: "{{baseUrl}}"
  body:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("Start team folder from clean auth state", function() {
          expect([200, 204]).to.include(res.status);
        });
