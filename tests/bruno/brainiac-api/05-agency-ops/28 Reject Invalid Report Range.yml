info:
  name: Reject Invalid Report Range
  type: http
  seq: 28

http:
  method: POST
  url: "{{rpcBaseUrl}}/agencyOps/reports/summary"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "json": {
          "teamId": "{{teamId}}",
          "from": "{{reportTo}}",
          "to": "{{reportFrom}}"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Invalid report range is rejected", function() {
          expect([400, 422]).to.include(res.status);
        });
