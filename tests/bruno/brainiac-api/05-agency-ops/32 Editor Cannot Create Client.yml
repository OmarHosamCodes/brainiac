info:
  name: Editor Cannot Create Client
  type: http
  seq: 32

http:
  method: POST
  url: "{{rpcBaseUrl}}/agencyOps/clients/create"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "json": {
          "teamId": "{{teamId}}",
          "name": "Editor Forbidden Client"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Editor is blocked from owner-only client create", function() {
          expect([401, 403]).to.include(res.status);
        });
