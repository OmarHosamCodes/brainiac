info:
  name: Editor Cannot Create Project
  type: http
  seq: 34

http:
  method: POST
  url: "{{rpcBaseUrl}}/agencyOps/projects/create"
  headers:
    - name: content-type
      value: application/json
  body:
    type: json
    data: |-
      {
        "json": {
          "teamId": "{{teamId}}",
          "clientId": "{{clientId}}",
          "name": "Editor Forbidden Project"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("Editor is blocked from owner-only project create", function() {
          expect([401, 403]).to.include(res.status);
        });
